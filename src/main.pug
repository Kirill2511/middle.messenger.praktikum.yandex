<!DOCTYPE html>
html(lang="ru")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Чат
    body
        header
            block nav

        main
            block content

        block scripts
            script(src='~/src/main.js' type='module')
